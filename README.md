A small setup that builds raylib bindings with the headers and binaries provided.  
Just add bindgen and run the Makefile! Output should be in the 'output' folder.

TODO:
- Change some types ("i32" -> "c.int")
- Add raygui binaries