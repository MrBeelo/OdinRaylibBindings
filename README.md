A small setup that builds raylib bindings with the headers and binaries provided.  
Just add bindgen and run the Makefile! Output should be in output.

TODO:
- raylib: change all fileDatas in *FromMemory to rawptr